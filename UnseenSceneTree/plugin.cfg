[plugin]

name="UnseenTree"
description="Editor scene tree now tells you what layer you are on and if scene tree can be expanded or not, instead of repeating name twice."
author="Ericbomb"
version="1.0"
script="plugin.gd"
